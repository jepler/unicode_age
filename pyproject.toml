[project]
name = "unicode_age"
version = "18.0.0.0"
description = "A package for determining what version a Unicode codepoint was added to the standard"
requires-python = ">=3.8"
license = "MIT"
readme = "README.md"

[project.optional-dependencies]
dev = [
    "pytest",
    "twine",
    "build",
]

[project.scripts]
unicode-age = "unicode_age.__main__:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.tox]
legacy_tox_ini = """
[tox]
envlist = py{39,310,311,312,313,314}

[testenv]
deps = pytest
commands = pytest
"""
